{
  "name": "Mk2PVRouter Documentation",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "features": {
    "ghcr.io/devcontainers/features/github-cli:1": {}
  },
  "postCreateCommand": "pip install --upgrade pip && pip install -U -r requirements.txt && git config --global url.\"https://github.com/\".insteadOf \"git@github.com:\"",
  "postAttachCommand": "gh auth setup-git 2>/dev/null || echo 'Run \"gh auth login\" to enable git push/pull'",
  "customizations": {
    "vscode": {
      "extensions": [
        "lextudio.restructuredtext",
        "trond-snekvik.simple-rst",
        "ms-python.python",
        "hediet.vscode-drawio"
      ]
    }
  }
}
